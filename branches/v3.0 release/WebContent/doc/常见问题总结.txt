【v3.x 需要解决的常见问题】
1、cookie 的支持问题
2、（搞定）设置多选中状态、多节点同时拖拽
3、（搞定）提供 setting.edit.drag.prev/next/inner 设置可以随意设置全部节点拖拽的状态，也可以设置function引用，针对节点自行控制拖拽定义
4、（搞定）通过接口操作节点时是否需要触发各种事件的思考(checkNode可以触发check事件；expandNode可以触发expand/collapse事件; removeNode可以出发click事件)
5、（搞定）增加 node.getCheckStatus 方法简单获取节点 checkbox/radio 的勾选、半勾选状态？
6、（搞定）async的时候，最好可以指定是get还是post.
7、（搞定）autoParam里指定要传参数的时候最好可以自定义***传递时候***用的参数名。比如，autoParam: ["ID"]，和服务器通信时传的就是ID=(ID的值)，要是这里可以自定义参数名CustomID=(ID的值)就好了。因为用json加载比较大的树的时候，如果用比较短的字段名，可以节省流量。但是postback的时候，数据一般较少，这时候用有意义的名字可以便于维护。
8、（搞定）增加 title 设置，对于其他个性化属性，可以利用zTree的 Dom 命名规则自行制作
9、（搞定）【bug】在没加open:false时，第一次检查节点的 open 属性时，为 undefined（Line 366 这个if条件有问题，应该增加isParent=true 的判断）
10、（搞定）当初始化传给zTree的是一个对象时，zTree不能正常显示
11、（搞定）数据初始化 & 节点显示优化问题
12、（搞定）增加 getPreNode、getNextNode 方法
13、（搞定）修正expandAll方法，当无参数时，进行toggle操作
14、（搞定）修正reAsyncChildNodes方法增加 isSilent 参数
15、（搞定）1像素的css问题搞定
16、css 重新调整 以及 img 制作
17、(放弃了) checkbox & radio 共存原生支持
18、（搞定）初始化数据自动进行 clone 操作避免用户反复初始化造成数据重复, addNodes方法会返回新数据
19、（搞定）对li、a、ul 增加 level class，可以利用 css 控制某一级节点样式（包括：隐藏+ -号、节点布局等）
20、（搞定）beforeClick、onClick事件回调函数增加 clickFlag参数，表明是选中还是取消选中
21、（搞定）如果是ie6，自动取消动画设置
22、（搞定）双击自动展开需要开关
23、（搞定）子节点展开只high变化！
24、（搞定）css自定义图标 可以兼容IE6
25、（搞定）为了解决部分朋友生成 json 数据出现的兼容问题，针对 节点数据初始化的 boolean 值（open、checked、isParent）支持 "false","true" 字符串格式的数据。
26、需要完善不显示父节点的switch功能
27、（搞定）在做异步树加载的时候，设置node的icon值，替换了默认显示的图标，loading的图标就不会显示了，不设置icon值的话，loading图标就正常。
28、（搞定）使用自定义图标功能时，异步加载无法切换为loading图标的bug
29、（搞定）异步加载 与编辑共存时，拖拽过快，会导致目标父节点不进行异步加载（拖拽、moveNode、copyNode、addNodes都解决了异步加载的问题，并且都支持 isSilent 模式）
30、(放弃了) 支持 treeNode.selected 属性，用于初始化时默认选中，删除 isSelectedNode 方法
    原因：selected属性牵扯面太广，初始化zTree、异步加载，新增节点、移动节点都会牵扯此属性，很难让该属性无障碍的适应所有规则，对于初始化默认选中节点完全可以利用增加特殊标识后，使用 getNodeByParam 或 getNodesByParam 获取节点后动态实现。
31、（搞定）配置使用radio输入框，当用js操作 updateNode 方法设置 radio被勾选时，radio勾选规则失效的bug
32、（搞定）增加 setting.view.autoCancelSelected 属性，设置配合 ctrl 键是否可以取消节点选择
33、（搞定）增加 removeChilds 方法，便于用户组合操作(removeNode/moveNode/removeChilds 都需要清理 radioCheckedList / curSelectedList 和 Caches)
34、（搞定）从底层修正 checkbox / radio 关联关系，对于有很多不显示checkbox的节点的案例，也能够保证正常关联 （check_False_Full / check_True_Full 被替换为 check_Child_State）


google ProjectHome

==_*<font color=red>zTree v3.0 beta 发布了，在性能、功能、代码结构方面都进行了很多改进，希望大家踊跃使用，谢谢。</font>*_==

==_*<font color=red>v3.0 无法向下兼容，但对于使用过 v2.6 的朋友来说，升级不会太难，详细升级对照文档，请参考 [http://www.baby666.cn/v3/faq.php#_101 zTree v2.6 - v3.0 升级必读]</font>*_==

==_*<font color=red>[http://www.baby666.cn Hunter's Home] zTree 网站全面改版，这次 API 先提供在线版本</font>*_==

=zTree 简介（JQuery Tree 插件）=

利用Jquery的核心代码，实现一套能完成大部分常用工作的Tree功能

    * zTree v3.0 将核心代码按照功能进行了分隔，不需要的代码可以不用加载

    * 采用了 延迟加载 技术，上万节点轻松加载，即使在 IE6 下也能基本做到秒杀

    * 兼容 IE、FireFox、Chrome、Opera、Safari 等浏览器

    * 支持 JSON 数据

    * 支持静态 和 Ajax 异步加载节点数据

    * 支持任意更换皮肤 / 个性化图标（依靠css）

    * 支持极其灵活的 checkbox 或 radio 选择功能

    * 提供多种事件响应回调

    * 灵活的编辑（增/删/改/查）功能，可随意拖拽节点，还可以多节点拖拽哟

    * 在一个页面内可同时生成多个 Tree 实例

    * 简单的参数配置实现 灵活多变的功能

=ZTree 最新更新日志=
*2011.09.05* v3.0 beta
   * 【修改】zTree 的 js 代码架构全面修改，并且拆分
   * 【修改】zTree 的 css 样式全面修改，对浏览器可以更好地兼容，同时解决了以前1个像素差的问题
   * 【优化】采用延迟加载技术，一次性加载大数据量的节点性能飞速提升
   * 【增加】支持多节点同时选中、拖拽
   * 【增加】checkNode、checkAllNodes 等多种方法
   * 【增加】IE6 自动取消动画展开、折叠的功能
   * 【修正】异步加载 & 编辑模式 能够更完美的共存
   * 【修正】setting 配置更加合理，并且增加了若干项配置参数
   * 【修正】treeNode 节点数据的属性更加合理，并且增加了一些方法
   * 【修正】拖拽操作更加灵活方便，更容易制定自己的规则
   * 【修正】其他若干修改，详细对比请参考 url：http://www.baby666.cn/v3/faq.php#_101