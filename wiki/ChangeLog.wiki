#labels 更新日志,Log,zTree
=ZTree 更新日志=

<font color=#041594>
*v1.0 - v1.x 计划*
  * 增加皮肤
  * 增加功能：     
   # 显示CheckBox时，对于CheckBox的样式，在父级被选择时，能判定子集是否全部被选择。
   # 对于节点Name属性，需要支持<>等特殊字符的正常显示，并且在 editable=true 状态时，能够双击编辑内容
   # 支持自定义鼠标右键菜单
</font>

*2010.06.14* v1.02
  * 对数据JSON对象增加了iconSkin属性支持，允许在css中预先设定若干图标定义，数据中只需要定义iconSkin的Name即可显示自定义图标。
          使用css自定义图标可以更灵活进行配置，并且容易避免icon属性值中由于相对路径造成的图片无法正常显示的问题。
          使用方法可参考API说明，以及example中基本效果的例子

*2010.06.08* v1.01
  * 修正example中两个例子页面的错误，由于多了一个逗号，导致在firefox的ie tab下或 360之类的浏览器下，例子页面无法正常执行。这里特别感谢实事哥提醒。

*2010.06.01* v1.0 <font color="red">迎六一，zTree v1.0 正式版 发布</font>
  * 整理css样式
  * 整理统一常量 例如: ul/switch/ico等
  * 增加对自定义节点图标的支持功能
  * 提供公共接口 增加、移动、删除节点数据； 
  * 提供公共接口 设置节点被选择
  * 提供公共接口 利用tID查找treeNode节点数据
  * 异步获取Tree节点数据后，增加 ZTREE_AJAX_SUCCESS 事件trigger
  * 异步获取Tree节点数据后，增加 ZTREE_AJAX_ERROR 事件trigger
  * 修正公共方法的参数，减少treeObjId
  * 修正CheckBox点击状态无法正常显示，以及刷新Tree后，无法保留CheckBox点击状态的Bug
  * 修正移动节点后level没有重置的Bug
  * 修正moveNode方法中没有校验目标节点是否为操作节点的子节点的Bug
  * 自动绑定Tree特有事件，不需要使用者另行加载。 
  * 代码精简以及重组，重新构造命名空间结构，规范常量、公共方法的名称
  * 制作 API 使用手册 完毕

*2010.05.16* v1.0 beta7
  * 增加功能支持js控制 展开、折叠某个节点；
  * 增加功能支持js控制 展开、折叠全部节点；
  * 代码结构适当重组，重新区分public、private的函数
  * 修改初始化的构造函数，允许将 Tree参数设置 以及 节点数据 分开传递
  * 修正IE下拖拽到Iframe上，出现不流畅的兼容问题

*2010.05.12* v1.0 beta6
  * 完善Ajax方式数据初始化功能，建立Tree对象时，如果传入的初始数据为空，且 isAjax=true，则自动从指定的ajaxUrl地址自动获取节点数据（参数会从ajaxParamOther中自动获取）
  * 修正由于W3Cdtd标准导致document.body.scrollTop无效问题，html页面是否使用W3C标准，都能正常定位拖拽图层
    # <font color=red>建议页面还是使用W3C标准，否则会影响其他显示效果。</font>
  * 修正拖拽层经过iframe时异常问题
  * 为避免数据混乱，在编辑状态，任何节点都不再支持异步加载子节点（根除外）
  * 展开、关闭节点时，自动修正open属性，保证在切换编辑、浏览状态时，树的显示节点无变化。

*2010.05.12* v1.0 beta5
  * 修正Css结构，保证IE、FireFox 兼容
  * 修正树的数据结构以及构造方法，可以在任意一级添加节点，并且可以将节点拖拽到根，成为根节点
  * 修正拖拽部分的代码结构，使拖拽的代码逻辑更合理，便于以后功能扩展
  * 同时还发现了一个FireFox v3.6.3的一个Bug，不知道是否以后能修正，目前该Bug已经利用代码绕过去了

*2010.05.08* v1.0 beta4
  * zTree的Html结构以及Css全面改版
   # 但目前Css的样式不支持IE，下一版将进行修正
   # 展开或折叠节点功能更改：点击‘+、-’图标 或 双击节点文字和图标
  * 修正Checkbox点击时，取消子节点选择时，针对父节点的联动Bug
  * 代码进行简单重组

*2010.05.04* v1.0 beta3
  * 修正滚动条位置影响拖动图层的坐标错误
  * 增加拖拽事件触发
  * click触发事件代码统一
  * 下一步要对树的css结构进行重组，重构树形线条的结构，并制作相应图片

*2010.04.29* v1.0 beta2
  * 加入Ajax功能，可以异步获取子节点
   # 将代码放入Tomcat发布目录下，可实现异步效果
  * 节点展开加入动画效果

*2010.04.27* v1.0 beta1
  * 尝试编写Jquery控件。
  * 全静态一次性初始化的Tree，尚未加入Ajax功能，将在1.0正式版本中实现